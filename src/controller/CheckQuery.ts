// import {QueryInterface} from "./QueryInterface";
// import InsightFacade from "./InsightFacade";
//
// export default class CheckQuery implements QueryInterface {
//     public static checkSemantics(obj: InsightFacade, query: any): boolean {
//
// }
//     public static checkSyntax(query:any): boolean {
//         if (this.checkBodySyntax(query:["WHERE"])) {
//             if (this.checkColumnsSyntax(query:["OPTIONS"]["COLUMNS"])) {
//                 if(Object.keys(query["OPTIONS"])).length == 2)
//             }
//         }
//     }
// }
